#include "cub.h"

int is_map_closed(t_game *game)
{
	char **temp_grid;
	int is_closed;

	temp_grid = ft_dup_map(game->map.grid);

	is_closed = flood_fill()
}

int ft_dup_map(t_game *game)
{

}

int flood_fill(t_game *game, temp_grid)

void free_tmp_map(t_game *game);
//duplicate map grid

//flood_fill

// free map matrix
